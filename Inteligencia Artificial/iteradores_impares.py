#Crear un iterdor para los números impares del 1 al 10
#Limite
limit = 10

#Iterador
odd_itter = iter(range(1,limit+1,2))

#Imprimir el iterador
for num in odd_itter:
    print(num)
    
    #Crear un iterdor para los números pares del 1 al 10
#Limite
limit = 10

#Iterador
even_itter = iter(range(0,limit+1,2))

#Imprimir el iterador
for num in even_itter:
    print(num)
    
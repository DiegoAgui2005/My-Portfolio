def generate_sales_report(month, sales):
    report = f"Sales for {month}: Total sales: ${sum(sales)}"
    
def genrerate_expenses(month, expenses):
    report = f"Expenses for {month}: Total expenses: ${sum(expenses)}"